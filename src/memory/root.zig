/// Memory safety utilities for memory access validation, sign-extension detection,
/// and instrumentation. Useful for emulators, debuggers, and memory-intensive applications.
pub const safety = @import("safety.zig");
