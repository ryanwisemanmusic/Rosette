name = "rosette-arm64-endianness"
version = "0.1.0-local"
kind = "source-mirror-endianness"
role = "source-mirror"
architectures = ["arm64"]
language = "zig"
verification = "verify.sh"

# Fixed-width byte-order and guest big-endian vectors are mirrored across the
# x86, ARM64, and PPC routes. Host properties remain explicit per route.
