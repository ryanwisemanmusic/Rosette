name = "xenia-x86-codegen-activation"
version = "0.1.0-local"
kind = "x86-64-codegen-and-activation-contract"
architectures = ["x86_64"]
language = "zig"
verification = "verify.sh"

# This package validates host-side facts. It is not an Xbyak replacement and
# it never turns an unresolved label or missing activation edge into success.
