name = "common-diagnostics-async-log-contract"
version = "0.1.0-local"
kind = "diagnostic-transport-policy-contract"
role = "build-time"
architectures = ["any"]
language = "zig"
verification = "verify.sh"
