name = "common-cocoa-window-admission-contract"
version = "0.1.0-local"
kind = "cocoa-window-admission-contract"
role = "build-time"
architectures = ["any"]
language = "zig"
verification = "verify.sh"
