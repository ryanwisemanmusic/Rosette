name = "common-xenia-wait-graph-contract"
version = "0.1.0-local"
kind = "guest-wait-signal-attribution-contract"
role = "build-time"
architectures = ["any"]
language = "zig"
verification = "verify.sh"
