name = "common-xenia-gpu-observation-contract"
version = "0.1.0-local"
kind = "gpu-observation-boundary-contract"
role = "build-time"
architectures = ["any"]
language = "zig"
verification = "verify.sh"
