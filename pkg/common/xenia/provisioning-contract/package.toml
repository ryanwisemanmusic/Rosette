name = "common-xenia-provisioning-contract"
version = "0.1.0-local"
kind = "console-platform-provisioning-custody-contract"
role = "build-time"
architectures = ["any"]
language = "zig"
verification = "verify.sh"
