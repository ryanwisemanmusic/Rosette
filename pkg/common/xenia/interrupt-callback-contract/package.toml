name = "common-xenia-interrupt-callback-contract"
version = "0.1.0-local"
kind = "emulator-interrupt-callback-contract"
role = "build-time"
architectures = ["any"]
language = "zig"
verification = "verify.sh"
